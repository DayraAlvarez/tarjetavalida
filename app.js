//Inicia programa pidiendo input a usuario por medio de prompt
var cardNumber = prompt ('Ingresa el numero de tarjeta');

var validNumber = function (plasticNumber){
  var str=numCard.toString ();
  var arr=str.split ('');
  arr.reverse();
  var prod=1;
  var sum=0;

  for(var i=o; i<arr.lenght;i++){
  
